import * as React from 'react';

export class UserCard extends React.PureComponent {
  render(): JSX.Element {
    return (
      <div className="user-card">
        <h2>User card</h2>
      </div>
    );
  }
}
